[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: Unit tests (fast, no external deps)
    integration: Integration tests (require DB)
    e2e: End-to-end tests (require running services)
    slow: Slow tests (LLM calls, large files)
