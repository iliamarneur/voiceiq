DATABASE_URL=postgresql://audio_knowledge:audio_knowledge@db/audio_knowledge
DEBUG=true
SECRET_KEY=your_secret_key

# Auth (opt-in, false by default for dev)
AUTH_ENABLED=false

# CORS (comma-separated origins, empty = allow all in dev)
ALLOWED_ORIGINS=

# Stripe billing
# Mode: "sandbox" (uses sk_test_) or "live" (uses sk_live_)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_MODE=sandbox
APP_BASE_URL=http://localhost:5173

# SMTP emails (optional, console stub if empty)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@clearrecap.com
